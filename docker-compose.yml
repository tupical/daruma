services:
  # ----------------------------------------------------------------------
  # Optional parity/runtime server image. Day-to-day Rust development runs
  # natively on the host; see Justfile.
  # ----------------------------------------------------------------------
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: daruma-server:dev
    container_name: daruma-server
    restart: unless-stopped
    environment:
      RUST_LOG: ${RUST_LOG:-info}
      DARUMA_DATA_DIR: /app/data
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4.1-mini}
    ports:
      # Bind to loopback only: external access goes through host nginx.
      # On VPS deploys, host nginx proxies https://<domain> → 127.0.0.1:<DARUMA_PORT>.
      - "127.0.0.1:${DARUMA_PORT:-8080}:8080"
    volumes:
      - daruma-data:/app/data

volumes:
  daruma-data:
