-- No-op placeholder. Migrations 0025-0027 were removed/renumbered during
-- development before release; this file preserves the version sequence so
-- databases that applied the original 0025-0027 (older dev binaries) still
-- open. Checksum drift vs the original is healed by reconcile_checksums in
-- db.rs (VersionMismatch path). Do not add schema changes here.
SELECT 1;
