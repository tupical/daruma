---
description: Start an daruma-claude run: parse the task, optionally decompose via AI, execute through omc team, and write artifacts back as daruma comments.
---

The user invoked `/daruma-claude:start`. Follow `${CLAUDE_PLUGIN_ROOT}/skills/start/SKILL.md`.
